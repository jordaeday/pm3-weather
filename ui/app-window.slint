export component MainWindow inherits Window {
    in property <float> temperature;
    in property <string> city-name;
    in property <int> weather-code;
    in property <string> state-name;
    
    Text {
        text: "Current temperature in " + city-name + ", " + state-name + ": " + temperature + "°F" + " (weather code: " + weather-code + ")";
        color: green;
    }
}